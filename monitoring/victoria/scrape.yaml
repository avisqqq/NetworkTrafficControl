global:
  scrape_interval: 10s

scrape_configs:
  - job_name: ntc
    static_configs:
      - targets:
          - host.docker.internal:8086  # dev: NTC on Mac
          # - localhost:8086           # rpi: NTC on same host (generated by deploy.sh)
    metrics_path: /metrics